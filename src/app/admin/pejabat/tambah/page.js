import PejabatForm from '../PejabatForm';

export const metadata = { title: 'Tambah Pejabat — Admin RS Bhayangkara' };

export default function TambahPejabatPage() {
  return <PejabatForm mode="create" />;
}
